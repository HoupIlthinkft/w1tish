name: Build Draft Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: windows
            artifact_patterns: |
              frontend/src-tauri/target/release/bundle/msi/*.msi
              frontend/src-tauri/target/release/bundle/setup/*.exe
          - os: macos-latest
            target: macos
            artifact_patterns: |
              frontend/src-tauri/target/release/bundle/dmg/*.dmg
              frontend/src-tauri/target/release/bundle/macos/*.app
          - os: ubuntu-latest
            target: android
            artifact_patterns: |
              frontend/src-tauri/gen/android/app/build/outputs/apk/universal/release/*.apk
              frontend/src-tauri/gen/android/app/build/outputs/bundle/universalRelease/*.aab

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Init Android depends
        if: matrix.target == 'android'
        run: |
          npm run tauri android init

      - name: Setup Java (for Android)
        if: matrix.target == 'android'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        if: matrix.target == 'android'
        uses: android-actions/setup-android@v3

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: wasm32-unknown-unknown

      - name: Install Rust Android targets
        if: matrix.target == 'android'
        run: |
          rustup target add aarch64-linux-android armv7-linux-androideabi i686-linux-android x86_64-linux-android

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      - name: Install dependencies (Ubuntu skipped for now, but we use macOS/Windows)
        working-directory: ./frontend
        run: |
          npm install

      - name: Install Tauri CLI
        run: cargo install tauri-cli

      - name: Build Windows app
        if: matrix.target == 'windows'
        working-directory: ./frontend
        run: npm run build:windows
        env:
          TAURI_BUILD: true

      - name: Build macOS app
        if: matrix.target == 'macos'
        working-directory: ./frontend
        run: npm run tauri build

      - name: Upload artifacts to draft release
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ matrix.artifact_patterns }}
          draft: true
          name: "Draft Release ${{ github.sha }}"
          tag_name: "draft-${{ github.run_number }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}