name: Build Draft Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: windows
            artifact_name: '*.exe *.msi'
          - os: macos-latest
            target: macos
            artifact_name: '*.app *.dmg'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: wasm32-unknown-unknown

      - name: Install dependencies (Ubuntu skipped for now, but we use macOS/Windows)
        run: |
          npm install --prefix frontend 

      - name: Install Tauri CLI
        run: cargo install tauri-cli

      - name: Build Windows app
        if: matrix.target == 'windows'
        run: npm run tauri build --prefix frontend 
        env:
          TAURI_BUILD: true

      - name: Build macOS app
        if: matrix.target == 'macos'
        run: npm run tauri build --prefix frontend 

      - name: Upload artifacts to draft release
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          name: "Draft Release ${{ github.sha }}"
          tag_name: "draft-${{ github.run_number }}"
          files: |
            src-tauri/target/release/bundle/**/${{ matrix.artifact_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}